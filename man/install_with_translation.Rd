% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install-with-translation.R
\name{install_with_translation}
\alias{install_with_translation}
\title{Build and install a translation module for a CRAN package's help}
\usage{
install_with_translation(
  pkg,
  language,
  translate = NULL,
  lib = .libPaths()[1],
  quiet = FALSE
)
}
\arguments{
\item{pkg}{package name (installed, or installable from CRAN)}

\item{language}{language code, e.g. "es"}

\item{translate}{optional \code{function(text)} returning the translation}

\item{lib}{library to install the module into}

\item{quiet}{suppress progress messages}
}
\value{
(invisibly) the installed module name, \code{paste0(pkg, ".", language)}
}
\description{
Given an installed CRAN package, fetch its matching source tarball, diff the
source Rd (dynamic \\Sexpr live) against the installed \code{Rd_db} (values
baked) to build per-topic scaffolds, translate the static text with
\code{translate}, and install the resulting \code{pkg.language} module. With
\code{translate = NULL} the module is a ready-to-fill template (every
translation left \code{NULL}). \code{translate} must be a
\code{function(text) -> text}; it only sees literal text between placeholders,
so it need not know about \code{{ISEXPR_i}} tokens.
}
