% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module-update.R
\name{i18n_module_update}
\alias{i18n_module_update}
\title{Update a translation module to a newer version of its package}
\usage{
i18n_module_update(module_path, package_path, dry_run = FALSE)
}
\arguments{
\item{module_path}{Path to the existing translation module.}

\item{package_path}{Path to the new version of the package source.}

\item{dry_run}{If \code{TRUE}, report what would change without writing anything.}
}
\value{
(invisibly) a list of counts: \code{unchanged}, \code{fuzzy}, \code{new}, \code{dropped}.
}
\description{
Re-syncs an existing translation module (made with \code{\link[=i18n_module_create]{i18n_module_create()}})
against a newer version of the package it translates. A fresh scaffold is
generated from the new source and merged into the existing module, per section
and per \verb{\\arguments} item:
}
\details{
\itemize{
\item \strong{unchanged} original -> the existing translation is kept;
\item \strong{changed} original -> the old translation is kept but the section is
flagged \code{fuzzy: yes}, with the previous text stored in \verb{previous_original:}
so the translator can see what changed and edit rather than redo;
\item \strong{new} section -> added with an empty \verb{translation:};
\item \strong{removed} section -> dropped.
}

At runtime a \code{fuzzy} section still shows its (possibly out-of-date) translation,
with a small note revealing the current original. The translator resolves it by
updating the \verb{translation:} and deleting the \verb{fuzzy:}/\verb{previous_original:}
lines; \code{i18n_module_update()} never clears the flag itself.

The module is updated \strong{in place} (it is a package, so review the change with
version control); \verb{man_original/} and the \verb{Translates:} version are refreshed.
Other DESCRIPTION fields (title, author, ...) are left untouched.
}
\seealso{
\code{\link[=i18n_module_create]{i18n_module_create()}}
}
